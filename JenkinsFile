pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh "./gradlew build"
            }
        }
        stage('Creating APK') {
            steps {
                echo './gradlew assembleDebug'
            }
        }
    }
}